<!DOCTYPE html>
<% const breadcrumbItems=[{ name: t.nav.home, href: link('/') }, { name: 'Lovart APK' , href: canonicalUrl }]; %>
    <html lang="<%= lang %>" class="scroll-smooth">

    <head>
        <%- include('partials/head') %>
            <meta charset="UTF-8">
            <meta name="viewport" content="width=device-width, initial-scale=1.0">
            <title>
                <%= t.seo.lovart_apk.title %>
            </title>
            <meta name="description" content="<%= t.seo.lovart_apk.description %>">
            <link rel="canonical" href="<%= canonicalUrl %>">
            <%- include('partials/hreflang') %>
                <link rel="stylesheet" href="/styles.css">
                <link rel="icon" href="/favicon.ico" type="image/x-icon">
                <script async
                    src="https://pagead2.googlesyndication.com/pagead/js/adsbygoogle.js?client=ca-pub-3083296102953963"
                    crossorigin="anonymous"></script>
                <%- include('partials/analytics') %>
                    <%- include('partials/schema-breadcrumb', { breadcrumbItems }) %>
                        <script type="application/ld+json"><%- JSON.stringify({
        "@context": "https://schema.org",
        "@type": "FAQPage",
        "mainEntity": t.lovart_apk_page.faq.items.map(function(f) {
            return { "@type": "Question", "name": f.q, "acceptedAnswer": { "@type": "Answer", "text": f.a } };
        })
    }) %></script>

                        <style>
                            /* Warning box */
                            .warning-box {
                                background: linear-gradient(135deg, #fff7ed 0%, #fff1e6 100%);
                                border-left: 5px solid #f97316;
                                border-radius: 0.75rem;
                                padding: 1.5rem;
                            }

                            .warning-badge {
                                display: inline-flex;
                                align-items: center;
                                gap: 0.375rem;
                                background: #f97316;
                                color: #fff;
                                font-size: 0.75rem;
                                font-weight: 700;
                                padding: 0.25rem 0.75rem;
                                border-radius: 9999px;
                                letter-spacing: 0.05em;
                                margin-bottom: 0.75rem;
                            }

                            .warning-item {
                                display: flex;
                                align-items: flex-start;
                                gap: 0.625rem;
                                margin-bottom: 0.625rem;
                                font-size: 0.9rem;
                                line-height: 1.5;
                            }

                            .warning-icon {
                                flex-shrink: 0;
                                color: #f97316;
                                font-weight: 700;
                                margin-top: 0.1rem;
                            }

                            /* Step cards */
                            .step-card {
                                background: #fff;
                                border: 1px solid #e5e7eb;
                                border-radius: 1rem;
                                padding: 1.5rem;
                                position: relative;
                            }

                            .step-num {
                                display: inline-flex;
                                align-items: center;
                                justify-content: center;
                                width: 2.5rem;
                                height: 2.5rem;
                                background: var(--color-primary);
                                color: #fff;
                                font-size: 0.85rem;
                                font-weight: 800;
                                border-radius: 9999px;
                                margin-bottom: 1rem;
                                flex-shrink: 0;
                            }

                            /* Compare cards */
                            .compare-fake {
                                background: #fff7f7;
                                border: 1.5px solid #fca5a5;
                                border-radius: 0.875rem;
                                padding: 1.5rem;
                            }

                            .compare-pwa {
                                background: #f5f3ff;
                                border: 1.5px solid #c4b5fd;
                                border-radius: 0.875rem;
                                padding: 1.5rem;
                            }

                            .compare-item {
                                display: flex;
                                align-items: flex-start;
                                gap: 0.5rem;
                                margin-bottom: 0.625rem;
                                font-size: 0.875rem;
                                line-height: 1.5;
                            }

                            .ci-icon {
                                flex-shrink: 0;
                                margin-top: 0.1rem;
                                font-size: 0.875rem;
                            }

                            /* FAQ */
                            details summary {
                                cursor: pointer;
                                list-style: none;
                            }

                            details summary::-webkit-details-marker {
                                display: none;
                            }

                            details[open] summary {
                                color: var(--color-primary);
                            }

                            .chevron {
                                transition: transform 0.2s;
                                display: inline-block;
                            }

                            details[open] .chevron {
                                transform: rotate(180deg);
                            }

                            /* Mobile-first step connector */
                            @media (min-width: 768px) {
                                .steps-grid {
                                    display: grid;
                                    grid-template-columns: repeat(3, 1fr);
                                    gap: 1.5rem;
                                }
                            }
                        </style>
    </head>

    <body class="bg-neutral-light text-neutral-dark antialiased">

        <%- include('partials/header') %>
            <%- include('partials/breadcrumb', { breadcrumbItems }) %>

                <main>

                    <!-- ═══ HERO ═══ -->
                    <section class="bg-gradient-to-b from-indigo-50 to-transparent pt-12 pb-16 lg:pt-20 lg:pb-24">
                        <div class="container mx-auto px-4 max-w-5xl">
                            <div class="grid lg:grid-cols-2 gap-8 items-center">
                                <div>
                                    <span
                                        class="inline-block py-1 px-3 rounded-full bg-primary/10 text-primary text-sm font-semibold mb-4">
                                        <%= t.lovart_apk_page.hero.badge %>
                                    </span>
                                    <h1
                                        class="text-3xl md:text-4xl lg:text-5xl font-bold text-neutral-dark mb-4 leading-tight">
                                        <%= t.lovart_apk_page.hero.title %>
                                    </h1>
                                    <p class="text-base md:text-lg text-neutral mb-6 leading-relaxed">
                                        <%= t.lovart_apk_page.hero.subtitle %>
                                    </p>
                                    <div class="flex flex-col sm:flex-row gap-3">
                                        <a href="<%= link('lovart-free') %>"
                                            class="inline-flex items-center justify-center px-6 py-3.5 bg-primary text-white font-semibold rounded-xl shadow-lg hover:bg-primary-dark transition-all text-base">
                                            <%= t.lovart_apk_page.hero.cta_primary %> →
                                        </a>
                                        <a href="<%= link('lovart-pricing') %>"
                                            class="inline-flex items-center justify-center px-6 py-3.5 bg-white text-primary font-semibold rounded-xl border-2 border-primary hover:bg-primary/5 transition-all text-base">
                                            <%= t.lovart_apk_page.hero.cta_secondary %>
                                        </a>
                                    </div>
                                </div>
                                <div class="flex justify-center lg:justify-end">
                                    <img src="/images/apk/pwa-install.png"
                                        alt="Installing Lovart as a PWA on Android Chrome — safe alternative to lovart apk"
                                        class="w-56 sm:w-64 lg:w-72 drop-shadow-2xl" width="300" height="560"
                                        loading="eager">
                                </div>
                            </div>
                        </div>
                    </section>

                    <!-- Ad Slot 1 -->
                    <div class="container mx-auto px-4 max-w-5xl my-6">
                        <%- include('partials/adsense', { format: 'horizontal' }) %>
                    </div>

                    <!-- ═══ SECURITY WARNING ═══ -->
                    <section class="container mx-auto px-4 max-w-4xl my-10">
                        <div class="warning-box">
                            <div class="flex items-start gap-4">
                                <img src="/images/apk/security-warning.png" alt="Fake Lovart APK malware warning"
                                    class="w-14 h-14 sm:w-16 sm:h-16 object-contain flex-shrink-0" width="64"
                                    height="64" loading="lazy">
                                <div class="flex-1">
                                    <span class="warning-badge">
                                        <%= t.lovart_apk_page.warning.badge %>
                                    </span>
                                    <h2 class="text-xl md:text-2xl font-bold text-orange-800 mb-4">
                                        <%= t.lovart_apk_page.warning.heading %>
                                    </h2>
                                    <% t.lovart_apk_page.warning.points.forEach(function(pt) { %>
                                        <div class="warning-item">
                                            <span class="warning-icon">▸</span>
                                            <span class="text-orange-900">
                                                <%= pt %>
                                            </span>
                                        </div>
                                        <% }) %>
                                </div>
                            </div>
                        </div>
                    </section>

                    <!-- ═══ KEYWORD BRIDGE ═══ -->
                    <section class="container mx-auto px-4 max-w-4xl my-4">
                        <p class="text-neutral text-sm leading-relaxed text-center max-w-2xl mx-auto">
                            Every week, thousands of users search for a <strong>lovart apk</strong> or <strong>lovart
                                android</strong> download link — and most land on sites that are either outdated or
                            outright dangerous. The truth is simple: there is no official <strong>lovart apk</strong>
                            file hosted anywhere. The Lovart team has not released a <strong>lovart apk</strong> for
                            Google Play or any third-party store. Instead, Lovart is delivered as a Progressive Web App
                            (PWA), which means the <strong>lovart android</strong> experience is accessible directly
                            from your browser — no <strong>lovart apk</strong> download required, and no security risk
                            involved.
                        </p>
                    </section>

                    <!-- ═══ INSTALLATION GUIDE ═══ -->
                    <section class="bg-white py-14 my-4">
                        <div class="container mx-auto px-4 max-w-5xl">
                            <h2 class="text-2xl md:text-3xl font-bold text-neutral-dark mb-2 text-center">
                                <%= t.lovart_apk_page.guide.heading %>
                            </h2>
                            <p class="text-neutral text-center mb-10 max-w-2xl mx-auto text-sm leading-relaxed">
                                <%= t.lovart_apk_page.guide.subheading %>
                            </p>

                            <!-- Mobile: vertical stack / Desktop: 3-col grid -->
                            <div class="space-y-4 md:space-y-0 steps-grid">
                                <% t.lovart_apk_page.guide.steps.forEach(function(step) { %>
                                    <div class="step-card">
                                        <div class="step-num">
                                            <%= step.num %>
                                        </div>
                                        <h3 class="text-base font-bold text-neutral-dark mb-2">
                                            <%= step.title %>
                                        </h3>
                                        <p class="text-neutral text-sm leading-relaxed">
                                            <%= step.desc %>
                                        </p>
                                    </div>
                                    <% }) %>
                            </div>

                            <!-- Mobile CTA below steps -->
                            <div class="text-center mt-8">
                                <a href="<%= link('lovart-free') %>"
                                    class="inline-flex items-center px-7 py-3.5 bg-primary text-white font-semibold rounded-xl shadow hover:bg-primary-dark transition-all">
                                    <%= t.lovart_apk_page.compare.cta %> →
                                </a>
                            </div>
                        </div>
                    </section>

                    <!-- ═══ PWA vs FAKE APK COMPARE ═══ -->
                    <section class="container mx-auto px-4 max-w-5xl my-14">
                        <h2 class="text-2xl md:text-3xl font-bold text-neutral-dark mb-3 text-center">
                            <%= t.lovart_apk_page.compare.heading %>
                        </h2>
                        <p class="text-neutral text-center mb-8 max-w-2xl mx-auto text-sm leading-relaxed">
                            Searching for a <strong>lovart apk</strong> or <strong>lovart android</strong> download is
                            dangerous. Unlike a legitimate <strong>lovart apk</strong>, the PWA method installs directly
                            from the official lovart.com domain — zero third-party risk.
                        </p>

                        <div class="grid md:grid-cols-2 gap-5">
                            <!-- Fake APK -->
                            <div class="compare-fake">
                                <h3 class="font-bold text-red-700 text-base mb-4">
                                    <%= t.lovart_apk_page.compare.fake_title %>
                                </h3>
                                <% t.lovart_apk_page.compare.fake_items.forEach(function(item) { %>
                                    <div class="compare-item">
                                        <span class="ci-icon text-red-400">✗</span>
                                        <span class="text-neutral-dark">
                                            <%= item %>
                                        </span>
                                    </div>
                                    <% }) %>
                            </div>
                            <!-- PWA -->
                            <div class="compare-pwa">
                                <h3 class="font-bold text-primary text-base mb-4">
                                    <%= t.lovart_apk_page.compare.pwa_title %>
                                </h3>
                                <% t.lovart_apk_page.compare.pwa_items.forEach(function(item) { %>
                                    <div class="compare-item">
                                        <span class="ci-icon text-primary">✓</span>
                                        <span class="text-neutral-dark">
                                            <%= item %>
                                        </span>
                                    </div>
                                    <% }) %>
                            </div>
                        </div>
                    </section>

                    <!-- Ad Slot 2 -->
                    <div class="container mx-auto px-4 max-w-4xl my-6">
                        <%- include('partials/adsense', { format: 'square' }) %>
                    </div>

                    <!-- ═══ FAQ ═══ -->
                    <section class="container mx-auto px-4 max-w-3xl my-14">
                        <p class="text-neutral text-center text-sm leading-relaxed mb-8 max-w-2xl mx-auto">
                            Many users searching for a <strong>lovart apk</strong> don't realize that a safe, official
                            alternative exists. The Lovart PWA delivers the full <strong>lovart android</strong>
                            experience — with all AI design tools — without any <strong>lovart apk</strong> installation
                            required.
                        </p>
                        <h2 class="text-2xl md:text-3xl font-bold text-neutral-dark mb-6 text-center">
                            <%= t.lovart_apk_page.faq.heading %>
                        </h2>
                        <div class="space-y-3">
                            <% t.lovart_apk_page.faq.items.forEach(function(faq) { %>
                                <details class="bg-white border border-neutral-light rounded-xl px-4 py-4">
                                    <summary
                                        class="flex justify-between items-start gap-3 font-semibold text-neutral-dark text-sm md:text-base">
                                        <h3 class="m-0 font-semibold text-left">
                                            <%= faq.q %>
                                        </h3>
                                        <span class="chevron text-primary flex-shrink-0 mt-0.5">▾</span>
                                    </summary>
                                    <p class="mt-3 text-neutral text-sm leading-relaxed border-t border-gray-100 pt-3">
                                        <%= faq.a %>
                                    </p>
                                </details>
                                <% }) %>
                        </div>
                    </section>

                    <!-- Ad Slot 3 -->
                    <div class="container mx-auto px-4 max-w-4xl my-6">
                        <%- include('partials/adsense', { format: 'horizontal' }) %>
                    </div>

                    <!-- ═══ FINAL CTA ═══ -->
                    <section class="bg-gradient-to-r from-primary to-indigo-500 py-16 mt-4">
                        <div class="container mx-auto px-4 text-center max-w-2xl">
                            <h2 class="text-2xl md:text-3xl font-bold text-white mb-3">
                                <%= t.lovart_apk_page.cta.title %>
                            </h2>
                            <p class="text-base text-white/80 max-w-lg mx-auto mb-7">
                                <%= t.lovart_apk_page.cta.desc %>
                            </p>
                            <a href="<%= link('lovart-free') %>"
                                class="inline-flex items-center justify-center w-full sm:w-auto px-8 py-4 bg-white text-primary text-lg font-semibold rounded-xl shadow-lg hover:bg-neutral-light transition-all">
                                <%= t.lovart_apk_page.cta.button %> →
                            </a>
                        </div>
                    </section>

                </main>

                <%- include('partials/footer') %>

                    <script>
                        const btn = document.getElementById('mobile-menu-button');
                        const menu = document.getElementById('mobile-menu');
                        if (btn && menu) btn.addEventListener('click', () => menu.classList.toggle('hidden'));
                        const yr = document.getElementById('currentYear');
                        if (yr) yr.textContent = new Date().getFullYear();
                    </script>

    </body>

    </html>